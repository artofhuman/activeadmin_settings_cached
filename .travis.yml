language: ruby
cache: bundler

gemfile:
  - gemfiles/rails5.0.gemfile
  - gemfiles/rails5.1.gemfile
  - gemfiles/rails6.0.gemfile

before_script:
  - bundle exec rake setup

script:
  - bundle exec rake

rvm:
  - 2.5.0
